use advent_of_code_2019::*;
use std::fs::read_to_string;

pub fn main(input: Option<&str>) -> Result<()> {
    let _input = read_to_string(input.unwrap_or("input/day15.txt"))?;

    // answer!(15, 1, 42);
    Ok(())
}
